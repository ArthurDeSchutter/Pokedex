{
  "name": "pokedex",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd pokedex && npm install && npm run build"

    },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pokedex-promise-v2": "^3.3.0"
  }
}
